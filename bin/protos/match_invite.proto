syntax = "proto3";
package palace;

message LobbyInfo {
    string host = 1;
    repeated PlayerEntry players = 2;
    bool canStart = 3;
    bool canJoin = 4;
}

message PlayerEntry {
    string userID = 1;
    bool ready = 2;
}

message UserIDs {
    repeated string ids = 1;
}